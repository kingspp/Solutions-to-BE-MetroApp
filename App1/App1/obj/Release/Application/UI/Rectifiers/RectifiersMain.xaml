<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="App1.RectifiersMain"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:common="using:App1.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>

        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:ConnectionId='7' x:Name="backButton"                IsEnabled="{Binding Frame.CanGoBack, ElementName=pageRoot}" Style="{StaticResource BackButtonStyle}"/>
            <TextBlock x:Name="pageTitle" Grid.Column="1" Text="Rectifiers" Style="{StaticResource PageHeaderTextStyle}"/>
        </Grid>
        <TextBlock x:ConnectionId='1' HorizontalAlignment="Left" Margin="104,196,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Half Wave Rectifier" VerticalAlignment="Top" Height="34" Width="208" FontSize="24"             />
        <TextBlock x:ConnectionId='2' HorizontalAlignment="Left" Margin="373,196,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Full Wave Rectifier" VerticalAlignment="Top" Height="34" Width="208" FontSize="24"            />
        <TextBlock x:ConnectionId='3' HorizontalAlignment="Left" Margin="686,196,0,0" Grid.Row="1" TextWrapping="Wrap" Text="Bridge Wave Rectifier" VerticalAlignment="Top" Height="34" Width="244" FontSize="24"            />
        <Image x:ConnectionId='4' HorizontalAlignment="Left" Height="132" Margin="104,31,0,0" Grid.Row="1" VerticalAlignment="Top" Width="208" Source="../../../Assets/halfwave.jpg"             />
        <Image x:ConnectionId='5' HorizontalAlignment="Left" Height="132" Margin="373,31,0,0" Grid.Row="1" VerticalAlignment="Top" Width="208" Source="../../../Assets/fullwave.gif" RenderTransformOrigin="0.529,0.523"            />
        <Image x:ConnectionId='6' HorizontalAlignment="Left" Height="132" Margin="686,31,0,0" Grid.Row="1" VerticalAlignment="Top" Width="208" Source="../../../Assets/bridge_rectifier.gif" RenderTransformOrigin="0.529,0.523"            />

        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>

